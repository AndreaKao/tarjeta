function principal(option){
	do{
		var respuesta = prompt("Ingrese el número de su tarjeta");
		if(respuesta = "" || ){
			alert("Ingrese un número válido")
		var = num

if(i*2)
	num = size < 2
			}
		var arr = letter
		if(respuesta = letter)
	